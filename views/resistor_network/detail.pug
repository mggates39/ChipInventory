extends ../component/detail

block component_specific
  p #[strong Resistance] #{data.resistance} !{data.unit_label}
  p #[strong Tolerance] #{data.tolerance} %
  p #[strong Power] #{data.power} W
  p #[strong Number of Resistors] #{data.number_resistors}
